import React from "react";

import * as styles from "./navbar.module.scss";

const Navbar = () => (
  <nav>
    <ul>
      <ls>Portfolio</ls>
      <ls>CV</ls>
      <ls>Contact</ls>
      <ls>About</ls>
    </ul>
  </nav>
);

export default Navbar;
